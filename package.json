{
  "name": "altered-carbon-cold-storage",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate.mjs",
    "build": "bash scripts/build.sh",
    "release:check": "npm test && npm run validate && npm run build"
  },
  "engines": {
    "node": ">=22"
  }
}
